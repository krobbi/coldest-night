# NightScript Version 2.
# Patrol Route
# Patrol route for 'alice' and 'bob' in test.area_bx.west.

while true {
	:5
	"alice" ~ "PAlB"
	"bob" -> "PBoB"
	"alice" > left
	"bob" > left
	:5
	"alice" ~ "PAlA"
	"bob" -> "PBoA"
	"alice" > left
	"bob" > left
}
