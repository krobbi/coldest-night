# NightScript Version 3.
# Redirect Terminal
# Cutscene for the redirect terminal in test/area_bx/west.

include "redirect_terminal_dialog";

const FLAG = "test/area_bx/west/barrier_state";

freeze();
show();

main_dialog();

menu {
	option get_redirect_option() {
		set_flag(FLAG, not get_flag(FLAG));
		redirected_dialog();
	}
	
	option get_cancel_option() {}
}

hide();
unfreeze();
