# NightScript Version 3.
# Flag Test
# Test for setting and getting flags.

freeze();
show();

const VALUE = getFlag("v3_test", "counter") + 1;
setFlag("v3_test", "counter", VALUE);

say(format("Counter: {0}", VALUE));

hide();
unfreeze();
