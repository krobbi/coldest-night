# NightScript Version 3.
# Flag Test
# Test for setting and getting flags.

const FLAG = "nightscript/v3/test/counter";

func incrementFlag(){
	return setFlag(FLAG, getFlag(FLAG) + 1);
}

freeze();
show();

var value = incrementFlag();
say(format("Counter: {0}", value));

hide();
unfreeze();
