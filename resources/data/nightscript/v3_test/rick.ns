# NightScript Version 3.
# Rick
# A small test.

var OPTION_A = "Give you up";
var OPTION_B = "Let you down";
var OPTION_C = "Run around";
var OPTION_D = "Desert you";

freeze();
show();

say("Choose wisely...");

var choice = "";

menu {
	option(OPTION_A) choice = OPTION_A;
	option(OPTION_B) choice = OPTION_B;
	option(OPTION_C) choice = OPTION_C;
	option(OPTION_D) choice = OPTION_D;
}

name("Never gonna");
say(choice);

hide();
thaw();
